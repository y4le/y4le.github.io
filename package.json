{
  "name": "y4le-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check main.js && node --check scripts/dev.mjs && node --check scripts/project-manifest.mjs && node --check scripts/rebuild-manifest.mjs && node --test scripts/project-manifest.test.mjs scripts/rebuild-manifest.test.mjs && node scripts/build.mjs --check",
    "dev": "npm run build && node scripts/dev.mjs",
    "dev:tailscale": "npm run build && node scripts/dev.mjs --tailscale",
    "manifest": "node scripts/rebuild-manifest.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
