{
  "name": "y4le-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check scripts/dev.mjs && node scripts/build.mjs --check",
    "dev": "npm run build && node scripts/dev.mjs",
    "dev:tailscale": "npm run build && node scripts/dev.mjs --tailscale"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
