# Hand-edited site configuration. npm run manifest never writes this file.

site:
  title: YaleThom.as
  link: https://yalethom.as

# Pinned display order, listed by project slug (the path segment of the
# canonical link). Pinned projects render first, in this order; every other
# project follows by recency: ongoing work first, then the most recent end
# date, then the most recent start date, with the slug as the final tiebreak.
# Slugs that match no project in projects.yaml are reported and ignored.
order: []
